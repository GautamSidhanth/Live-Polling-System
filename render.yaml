

services:
  # Backend Service
  - type: web
    name: live-polling-server
    env: node
    region: singapore
    plan: free
    buildCommand: cd server && npm install && npx prisma generate && npx prisma db push --accept-data-loss && npm run build
    startCommand: cd server && npm run start
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false

  # Frontend Service
  - type: web
    name: live-polling-client
    env: static
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: client/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: live-polling-server
          property: host
